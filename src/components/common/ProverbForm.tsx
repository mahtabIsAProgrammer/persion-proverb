import { Grid, type SxProps, type Theme } from "@mui/material";

export const ProverbForm = () => {
  return <Grid sx={proverbFormSX}>ProverbForm</Grid>;
};

const proverbFormSX: SxProps<Theme> = {};
